using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class winScreenAudio : MonoBehaviour
{
    /*
    public AudioSource softLullaby;
   public AudioSource babyCoos;
   // AudioSource distortedLullaby;

    // Start is called before the first frame update
    void Start()
    {
        //softLullaby = GetComponent<AudioSource>();
       // babyCoos = GetComponent<AudioSource>();
    }

    // Update is called once per frame
    void Update()
    {
        if ((GameObject.Find("-player").GetComponent<playermovement>().lv2teleported == true))
        {

            GameObject.Find("crib").GetComponent<GameMaster>().distortedLullaby.Stop();
            

                //if (softLullaby.isPlaying == false && babyCoos.isPlaying == false)
                //{
                    softLullaby.Play();
                    babyCoos.Play();
               // }
            

        }
    }
    */
}
